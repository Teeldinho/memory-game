export const GAME_DELAYS = {
  MATCH_OVERLAY_MS: 1000,
  MEMORIZE_CARDS_MS: 5000,
} as const;

export const GAME_RULES = {
  JOKER_NAME: "joker",
  REQUIRED_SELECTED_CARDS: 2,
  SHUFFLE_ITERATIONS: 3,
} as const;
